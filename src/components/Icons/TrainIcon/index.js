import React from 'react';
import { Icon } from '@ui-kitten/components';

const TrainIcon = (props) => {
    return (
        <Icon {...props} name={'train'} pack={'material'} />
    );
};

export default TrainIcon;
