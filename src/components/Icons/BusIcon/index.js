import React from 'react';
import { Icon } from '@ui-kitten/components';

const BusIcon = (props) => {
    return (
        <Icon {...props} name={'directions-bus'} pack={'material'} />
    );
};

export default BusIcon;
