import React from 'react';
import { Icon } from '@ui-kitten/components';

const TramIcon = (props) => {
    return (
        <Icon {...props} name={'tram'} pack={'material'} />
    );
};

export default TramIcon;
