import React from 'react';
import { Icon } from '@ui-kitten/components';

const SubwayIcon = (props) => {
    return (
        <Icon {...props} name={'subway'} pack={'material'} />
    );
};

export default SubwayIcon;
