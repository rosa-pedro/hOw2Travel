import React from 'react';
import { Icon } from '@ui-kitten/components';

const FerryIcon = (props) => {
    return (
        <Icon {...props} name={'directions-boat'} pack={'material'} />
    );
};

export default FerryIcon;
