import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
        /*backgroundColor: 'red',*/

    },
    name: {
        marginHorizontal: 16,
    },
});

export default styles;
