import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    header: {
        padding: '3%',
        flexDirection: 'row',
        alignItems: 'flex-end',
    },
    icon: {
        width: 28,
        height: 28,
    },
    title: {
        paddingLeft: 8,
    }
});

export default styles;
